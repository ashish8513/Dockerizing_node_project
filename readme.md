# Kafka Docker Setup

A simple and clean Docker setup for running **Nginx Kafka** locally using Docker Compose.

---

## 🚀 Prerequisites

Before starting, make sure the following are installed:

- Docker
- Docker Compose

---

## 📦 1. Clone the Repository

```bash
git clone https://github.com/ashish8513/Dockerizing_node_project.git

cd Dockerizing_node_project


Make sure Docker Desktop is running, then execute:

docker compose up -d

```
